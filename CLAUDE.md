# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

Marketing site for **MobileREMS by Longevia** — mobile bone quality assessment (Echolight REMS®) in Western Washington. A multi-page **static** site: hand-written HTML per page, one shared `assets/css/styles.css`, one shared `assets/js/site.js`. No framework, no bundler, **no build step**. Deployed on Netlify (auto-deploy from `main`, publish dir = repo root).

## Commands

There is no build, lint, or test tooling. To preview locally, serve the repo root over HTTP (clean-URL redirects won't apply, but pages load):

```
python3 -m http.server 8000   # then open http://localhost:8000/index.html
```

## Conventions that span multiple files

These are the rules that aren't obvious from any single file and are easy to get wrong:

- **Cache-busting `?v=N`** — `styles.css` and `site.js` are referenced as `/assets/css/styles.css?v=2` and `/assets/js/site.js?v=2`. Filenames are NOT content-hashed and `netlify.toml` sets `/assets/*` to `must-revalidate`. Whenever you edit a file under `assets/`, **bump the `?v=` number on every `<link>`/`<script>` across all HTML pages** (they must stay in sync) or deployed pages load stale CSS/JS. This is why the recent git history is full of "bust asset cache" commits.

- **Clean URLs** — pages are linked without `.html` (e.g. `<a href="/technology">`). Each works only because `netlify.toml` has a `200` rewrite from `/technology` → `/technology.html`. **Adding a new page means adding a matching `[[redirects]]` block** in `netlify.toml`.

- **Shared nav is duplicated, not included** — there is no templating. The top bar, header nav, and mobile drawer markup are copy-pasted into every page; the `.nav-links` block, `.nav-cta`, `.drawer`, and footer are byte-identical across all 9 HTML pages **except per-page `active` classes** (index.html additionally keeps a few cosmetic blank lines / `<!-- -->` comments from its original authoring — functionally identical). Adding/renaming a nav link means editing every HTML file. `site.js` expects `#header`, `#burger`, `#drawer`, `#drawer-close` on each page.
  - **Top nav (5 items):** The Technology · **Learn ▾** · Who It's For · About · Research. "Learn" is a `<button class="nav-sub-trigger">` (styled to match the sibling links) inside `.nav-item.has-sub`; the `.nav-sub` dropdown opens on hover/focus and on touch via `:focus-within` (**no JS**). Its three items: REMS Technology → `/technology`, How REMS® Works → `/how-rems-works`, What to Expect → `/what-to-expect`. `/technology` is intentionally reachable both from the top "The Technology" link and from the dropdown (owner decision).
  - **Active states:** `/technology` → "The Technology" top link + "REMS Technology" sub both `active` (trigger not); `/how-rems-works` & `/what-to-expect` → the Learn trigger + their sub `active` (top "The Technology" link not). Other pages keep their single active link.
  - **Drawer:** keeps the "The Technology" link, then a `.dl-group` (label "Learn" + 3 indented `.dl.sub` links), then Who It's For / About / Research / Book a Scan. Always expanded, no JS.
  - **Header CTAs:** "Partner With Us" is a **gold** button (`.btn-gold`) and "Book a Scan" is the dark `.btn-primary` (gold + dark pair). Body-content buttons keep their own styles (ghost CTAs elsewhere are unchanged).
  - **Mobile CTA / collapse:** the nav collapses to the burger at **≤1080px** (raised from 1000px to fit the 5th item); at that width "Partner With Us" (`.nav-cta .btn-gold`) stays pinned compact in the header while "Book a Scan" (`.btn-primary`) is hidden and lives in the drawer. The Partner label swaps to "Partner" at ≤380px via `.lbl-full`/`.lbl-short`. (The mobile compact/label-swap selectors target `.nav-cta .btn-gold`, retargeted when the button changed from ghost to gold.)

- **Lead forms are embedded Ivorey / GoHighLevel iframes** — not our own. `book.html` embeds **Get Early Access** (`86e7Pc45jHgMd5xxlOBd`); `partners.html` embeds **Partner With Us** (`qJDn7ir2vDwOw6re8NFz`). Each iframe (`.ghl-embed`, wrapped in the existing `.form-card`) needs `https://links.ivorey.io/js/form_embed.js` loaded **once per page**, right after the `site.js` `<script>` — the script auto-resizes the iframe to fit (the inline/CSS `min-height` is only the pre-load fallback). Submissions go straight to Lesya's CRM (Ivorey/GHL); a single lead channel, no duplicates. **We no longer use Netlify Forms** — `forms.html` is removed and `site.js` has no form handler (it only does nav-shadow + the mobile drawer). The `/partners` hero form card carries `.is-loading` with a shimmer `.form-loading` skeleton ("Loading the secure partnership form…") sitting *behind* the transparent iframe; `site.js` removes `.is-loading` on the iframe's `load` event (with a 6s fallback). This is a **loading-state UX only** — it does not speed up the third-party Ivorey load.

- **Logo assets** — `assets/img/logo-full.svg` is the full vertical lockup (keyhole + RF waves + wordmark, ~square 192×192); used large on the About `.brandblock`. `assets/img/logo-mark.svg` is the icon only (keyhole + waves, cropped viewBox, no wordmark); used as the `<img>` inside every header `a.brand` (the header stays horizontal: mark + text). Both are gold `#D4B985`. If you regenerate either, keep the gold in sync with `--gold`.

- **Home hero is the founder photo** — `index.html`'s `.hero-media > .keyframe.has-photo` shows `assets/img/founder-hero.jpg` (852×1065, 4:5) in a **rectangular frame with uniform 18px corners** (the old keyhole-arch `border-radius` was removed; `.keyframe` has `overflow:hidden`, so the photo, `.hero-photo-tone` warm overlay, gold inner-edge `inset` box-shadow, and `::after` noise layer all follow the same rounded-rectangle shape). The earlier animated spine-scan SVG (`.hero-scan` / `.hs-*` classes + `@keyframes hs*`) was **removed** from both HTML and CSS — it was not moved to `/technology`; it lives in git history if ever needed. On mobile the `.hero-inner` carries `var(--gutter)` side padding (the base rule zeroes horizontal padding, so the `≤980px` rule restores it) so the hero copy/buttons aren't flush to the screen edge.

- **Founder video** — Alexandra's YouTube Short (`JejpUNvq6Co`, vertical) plays via a lightweight `.video-facade` `<button>` (YouTube `hqdefault.jpg` poster + a `.vf-play` button); `site.js` swaps it for a `youtube-nocookie.com` autoplay iframe (`.vf-iframe`) on click. It now appears **only on the home page, block 03 "How REMS works"** `.teaser .media` (caption "Watch · How REMS works"; the body's "View the brochure" button still opens `/assets/clinical-brief.pdf`). Block 03 reuses the founder Short id for now — swap the `data-yt` if a dedicated explainer is produced.
- **Founder photo** — `assets/img/founder-probe.jpg` (Alexandra with the REMS probe, 853×1066) is shown as `.founder-photo` in **two** places: the home **block 06 "Why MobileREMS"** (`.founder .pic`) and the **`/about` B2C column** (`.about-b2c .pic`) — both captioned "Founder · Alexandra Kusherets · Certified REMS® Operator". The `.founder-photo` fill rule (`object-fit:cover; object-position:50% 28%`) covers both selectors, and both `.pic` boxes get the subtle gold inner-edge inset to match the hero photo. (`/about` used to show the video facade; it was swapped to this photo because the YouTube thumbnail framed badly.) The old `.brochure-cover` component is gone; `assets/img/brochure-cover.jpg` is now unused.

- **`/about` is a B2C + B2B page** — order: page-hero → `.about-split` → "Who's behind the scan" → optional coaching → `.brandblock` → final CTA. The `.about-split` is two columns: **left (B2C)** = the founder photo + personal story + a Book a Scan button; **right (B2B)** = an `.about-b2b` sticky card pitching the provider offer (Partner With Us + Clinical Brief). Then a `.aud-grid` 2×2 "Who's behind the scan" (provider-facing bio) and an optional coaching section. Stacks to one column at ≤980px. **Copy-accuracy flag:** Alexandra's bio claims here (work with Allergan Aesthetics, "twenty years", "a decade in nutrition", certified Echolight REMS® operator, certified health coach) and "Longevia" vs "Longevia Center" must be owner-verified before/with the physician outreach (same spirit as the provider-stats FTC note). The vendor spelling is **Echolight REMS®** (the source doc's "Ecolight" was a typo).

- **Learn cluster (9 pages total)** — `/technology` is the **hub**: hero → "Where REMS® Fits" (with a `.duo` two-up "What REMS Reads") → the kept benefits grid / four data points / REMS-vs-DEXA table → a "How REMS works" teaser (→ `/how-rems-works`) → a "What to expect" teaser (→ `/what-to-expect`) → "The evidence" (→ `/research`) → Clinical Reference + brochure downloads + the two videos → a two-path CTA. Two new sub-pages hang off it: **`/how-rems-works`** (mechanism, with the `.video-facade` id `D3AswHf6zBo` and a `.step-list` of 5 steps) and **`/what-to-expect`** (before/during/after using `.spec-list`, then 3 benefit cards). Sub-pages are reached via the hub teasers + the footer "Explore" column (now: The Technology / How REMS® Works / What to Expect / Who It's For / About / Research) — they are **not** added to the top nav, which keeps its 4 items. Clean-URL redirects for both are in `netlify.toml`. New CSS components: `.duo`, `.step-list`/`.step`, `.spec-list`/`.spec-item`.
- **Clinical Reference PDF** — `assets/clinical-reference.pdf` is a NEW open (no-gate) provider PDF, **distinct** from the Bone Identity brochure `clinical-brief.pdf`. Linked (new tab) from the `/technology` hub and `/how-rems-works`. Many provider stats trace to it (e.g. Cortet 2021 n=4,307 backs the "4,300+ patients" claim; LSC ~1%; sensitivity/specificity).
- **Learn-cluster owner-verify flags** (resolve before/with outreach): phone is the site's **(425) 200-4150** (her mockup's 425-681-5838 was NOT used); scan duration is **"about 30 minutes"** (her mockup's 45 min was a conflict); entity is **"Longevia"** in prose ("Longevia Health Center" only if that's the legal name). Also the Clinical Reference copy says "12-page" but the shipped PDF is 8 pages — confirm which file is final.

- **Brochure PDF** — the Echolight "Bone Identity" brochure lives at `/assets/clinical-brief.pdf` (canonical URL). The home "How REMS works" `.teaser` shows `assets/img/brochure-cover.jpg` as a `.brochure-cover` link (gold-framed on the cream mat) that opens the PDF in a new tab; the teaser's button is now "View the brochure" and also opens the PDF (it no longer links to `/technology` — that page is still reachable from the nav). The `.dl-cta` "Download brochure" button on `/technology` and the "Download the Clinical Brief" button on `/partners` both point to the PDF too. All brochure links use `target="_blank" rel="noopener"`. The "Book a 15-minute call" button on `/partners` stays disabled (no Calendly yet).

- **`/partners` is the "For Providers" page** — provider-focused, and its `<head>` meta (title / description / og:title / og:description) reflects that ("For Providers · Bring REMS® to Your Practice", a provider-focused description). The hero is a **two-column split** (`.provider-hero`, grid `1fr 1.15fr`): For-Providers copy on the left (with a `.cred-panel` credentials list — gold check icons, bold metrics), the Ivorey "Partner With Us" form on the right. The rest of the page covers the four readings, what it does for the practice, patient groups, and how it works. **The hero form is the ONLY "Partner With Us" form on the page** — the old bottom "Request a partnership" section was removed so `form_embed.js` only loads one embed (faster). The single hero iframe keeps id `inline-qJDn7ir2vDwOw6re8NFz-hero` and the CTA anchors to `#partner-form-hero`. **Geography on this page only is "South Puget Sound and Greater Seattle"**; every other page keeps "Western Washington" / "Gig Harbor". The credentials wording is owner-approved but still unsourced (see the FTC note above); the global `.disclaimer` stays.

## Copy & brand rules (not visible in files)
- Gold is #D4B985 (the official logo gold; the whole site was aligned to it). The prior value #C0A878 is now **fully gone** — no hard-coded `#C0A878` / `%23C0A878` / `rgba(192,168,120,…)` remains in any HTML or CSS (favicon, pulse keyframe, and inline `.rf` SVGs were all updated). The brand book's #D4C8CA is a typo — never use it anywhere.
- Banned AI words in copy: journey, holistic, wellness, game-changer, seamless. Avoid em-dash as a stylistic device. US English. Vendor name is always "Echolight REMS®".
- Do NOT add the claim "a decade of biological age reversed" (or variants) anywhere on the site — intentionally omitted from /about pending founder sign-off (FTC/medical-claim risk).
- **Provider-page stats are unsourced.** The `/partners` proof bar and copy ("Validated against DXA in 4,300+ patients", "Sub-0.5% precision error", "FDA-cleared", "the only REMS® provider in Western Washington") are owner-approved for the current pre-advertising site but **need sources before any paid advertising** (FTC). The global `.disclaimer` must stay on the page.

## Brand / design tokens

All color, type, and spacing live as CSS custom properties in `:root` at the top of `assets/css/styles.css` (palette porcelain/cream/charcoal + logo gold `--gold: #D4B985`, was `#C0A878`; darker accent `--bronze: #9C7B33`; serif display Cormorant Garamond, sans body Proxima Nova/Montserrat). Use the variables (`--gold`, `--ink`, `--bg`, etc.) rather than hard-coding values.
